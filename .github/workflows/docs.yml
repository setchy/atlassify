name: Docs

on:
  push:
    branches:
      - main
    paths:
      - "docs/**"
  pull_request:
    paths:
      - "docs/**"

permissions: {}

jobs:
  lint:
    name: Lint Docs
    uses: ./.github/workflows/lint.yml
    permissions:
      contents: read
    with:
      directory: "docs"
